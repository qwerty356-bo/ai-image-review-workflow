# AI 图片审核工作流 - 使用说明

## 一、项目概览

本工作流用于批量审核 AI 生成的古诗词背景图片，从 5 个维度自动评估：

- **画风** — 分类与置信度
- **画面质量** — 小数点后两位评分（0.00-10.00）
- **内容合规** — 是否涉及敏感元素
- **诗词契合度** — 图片意境与诗词内容的匹配度
- **AI 文字检测** — 是否有文字、文字是否正确

审核完成后生成：

- **HTML 在线报告**（浏览器打开，含统计概览 + 缩略图网格 + 明细表）
- **Excel / CSV 导出文件**（带格式化，可分发）

> 另有独立的**视频审核**流程（6 维度，含史实准确性），见 `video_batch_run.py` 等文件。

## 二、文件结构

```
image-review/
├── config.py            ← 图片审核配置（改这里适配你的环境）
├── video_config.py      ← 视频审核配置（独立，不影响图片）
├── poems_data.py        ← 119 首诗名→意境关键词映射表
├── review.py            ← 图片核心审核逻辑（单张：读图→调API→解析结果）
├── batch_run.py         ← 图片批量执行入口（★你运行的主文件）
├── generate_report.py   ← 生成图片 HTML 报告
├── export_excel.py      ← 导出图片 Excel/CSV
├── frame_extractor.py   ← 视频抽帧
├── video_review.py      ← 视频单帧审核
├── aggregation.py       ← 视频帧聚合
├── video_batch_run.py   ← 视频批量执行入口
├── generate_video_report.py ← 生成视频 HTML 报告
├── export_video_excel.py    ← 导出视频 Excel/CSV
├── requirements.txt     ← Python 依赖清单
├── .env.example         ← 环境变量配置模板（复制为 .env）
└── output/              ← 结果输出目录（自动创建）
    ├── results.json     ← 审核数据（增量保存）
    ├── report.html      ← HTML 报告（双击浏览器打开）
    ├── report.xlsx      ← Excel 导出
    ├── report.csv       ← CSV 导出
    └── thumbnails/      ← 缩略图目录
```

## 三、环境准备（只需做一次）

### 第 1 步：安装 Python 依赖

在项目目录打开终端，执行：

```bash
pip install -r requirements.txt
```

看到 `Successfully installed` 字样说明安装成功。

> 若提示 `pip` 不是内部命令，说明 Python 没加入系统 PATH，请用你本机 Python 的完整 pip 路径执行。

### 第 2 步：配置 API Key（二选一）

**方式 A：环境变量（推荐）**——复制 `.env.example` 为 `.env`，填入真实 API Key 与素材路径。程序自动读取。

**方式 B：直接改 `config.py`**——把 `API_KEY` 改成真实密钥，把 `IMAGE_DIR` 改成图片文件夹路径。

> ⚠️ 若直接改配置，请勿把含真实密钥的 `config.py` 提交到公开仓库。

## 四、运行流程

整个流程只需依次运行 3 个脚本，按顺序执行即可：

### 第 1 步：批量审核（耗时最久）

```bash
python batch_run.py
```

你会看到这样的终端输出：

```
============================================================
AI图片审核工作流 - 批量执行
============================================================
图片目录:   <你的图片目录>
图片总数:   238 张
使用模型:   doubao-seed-evolving
...
============================================================

[OK] ████████████░░░░░░░░░░░░░░░░░░░ 160/238 67.2% | 春望_bg3_2.png | A | 9.20 | 1.2张/s | ETA:65s
```

进度条实时更新，审核完的图片自动保存到 `output/results.json`。

> **断点续跑**：如果中途关闭，再次运行 `python batch_run.py` 会自动跳过已审核的图片，从断点继续。
>
> **只跑前 N 张测试**：`python batch_run.py --test 10` 只审核前 10 张。
>
> **重试失败的图片**：`python batch_run.py --retry-failed` 只重跑之前报错的。

### 第 2 步：生成 HTML 报告

```bash
python generate_report.py
```

生成 `output/report.html`，包含：

- 顶部统计概览（总数、各级别数量、合格率）
- 缩略图网格（按级别颜色标注，可筛选、点击放大）
- 画风分布饼图
- 完整明细表（可排序）

### 第 3 步：导出 Excel/CSV

```bash
python export_excel.py
```

生成 `output/report.xlsx` 和 `output/report.csv`。

Excel 文件包含 3 个 Sheet：

- **审核明细** — 全量数据表
- **统计汇总** — 分级统计、画风分布
- **异常图片** — 不合规或低分图片清单

## 五、查看结果

在文件管理器中进入 `output/` 文件夹：

1. **双击 `report.html`** → 浏览器打开可视化报告（建议用 Chrome/Edge 直接打开）
2. **双击 `report.xlsx`** → Excel 打开表格数据
3. `report.csv` → 纯文本格式，任何编辑器都能打开

## 六、配置说明（需要改什么）

打开 `config.py`，大部分配置已为你设好，你可能需要改的：

| 配置项 | 说明 |
| --- | --- |
| `API_KEY` | API 密钥（或用 `.env` 环境变量） |
| `IMAGE_DIR` | 图片所在文件夹路径 |
| `OUTPUT_DIR` | 结果输出目录（默认代码目录下 `output/`） |
| `MAX_WORKERS` | 并发数，增大可加快但可能触发限速 |
| `IMAGE_MAX_SIZE` | 图片压缩长边，减小可省 token |
| `WEIGHTS` | 各维度权重 |
| `GRADE_THRESHOLDS` | 分级标准，可调整阈值 |

**如果你要审核别的图片文件夹，只需改 `IMAGE_DIR` 一行即可。**

## 七、常见问题

### Q: 运行报错 "ModuleNotFoundError"

依赖没装。重新执行 `pip install -r requirements.txt`。

### Q: 运行报错 "403 Forbidden"

检查 `API_KEY` 与 `API_BASE_URL` 是否正确（可在 `.env` 中配置）。若网关对 `User-Agent` 有校验，请在 `.env` 中设置 `API_USER_AGENT`。

### Q: 审核中途断了怎么办

直接重新运行 `python batch_run.py`。系统会自动读取 `output/results.json` 中已审核的记录，跳过这些图片，从断点继续。

### Q: 某些图片报错怎么办

跑完后执行 `python batch_run.py --retry-failed`，只重试报错的图片。如果反复失败，检查该图片文件是否损坏或格式异常。

### Q: 想重新审核所有图片怎么办

删除 `output/results.json` 文件（或整个 `output` 文件夹），重新运行即可。

### Q: 换了 API Key 怎么办

改 `.env` 中的 `IMAGE_API_KEY`，或 `config.py` 中的 `API_KEY`，删掉 `output/results.json`，重新运行。

### Q: 视频审核怎么跑

把视频放进 `VIDEO_DIR`（`video_config.py` 中配置），确认 ffmpeg 路径正确，然后依次运行 `video_batch_run.py`、`generate_video_report.py`、`export_video_excel.py`。

## 八、完整运行命令速查

```bash
# 1. 安装依赖（首次）
pip install -r requirements.txt

# 2. 批量审核（图片）
python batch_run.py

# 3. 生成 HTML 报告
python generate_report.py

# 4. 导出 Excel/CSV
python export_excel.py

# ---- 视频审核 ----
# python video_batch_run.py
# python generate_video_report.py
# python export_video_excel.py
```

就这 4 条命令，依次执行即可。
